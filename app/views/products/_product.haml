.product
  - if product.photo?
    = image_tag product.photo.url(:small)
  = link_to product.send("name_#{current_locale}"), [product.category, product]
  .summary= product.send("summary_#{current_locale}_html")