= title @category.send("name_#{current_locale}")
.description= @category.send("description_#{current_locale}_html")

= render collection

- admin_section do
  - if @category
    = link_to "Uredi kategorijo", edit_category_path(@category)
    = link_to "Nov produkt", new_category_product_path(@category)
