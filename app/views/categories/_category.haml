%li
  - if category.photo and category.photo.photo.file?
    = image_tag category.photo.photo.url(:small)
  = link_to category.send("name_#{current_locale}"), category_products_path(category)
  .abstract= category.send("abstract_#{current_locale}")